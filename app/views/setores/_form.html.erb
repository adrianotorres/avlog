<%= form_for(setor) do |f| %>
  <% if setor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(setor.errors.count, "error") %> prohibited this setor from being saved:</h2>

      <ul>
      <% setor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :sigla %>
    <%= f.text_field :sigla %>
  </div>

  <div class="actions">
    <!-- <%= f.submit 'Salvar',class: "" do  %> <i class="fa fa-download"></i> Salvar <%end%> -->


    <%= button_tag( :class => "btn btn-info") do %>
      <i class="fa fa-download"></i> Salvar
    <% end %>
  </div>
<% end %>
