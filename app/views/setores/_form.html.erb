<%= bootstrap_form_for(setor, inline_errors: false) do |f| %>
  <%= f.text_field :nome %>
  <%= f.text_field :sigla %>
  <%= button_tag( :class => "btn btn-info") do %>
    <i class="fa fa-download"></i> Salvar
  <% end %>
  <%= link_to setores_path, class: "btn btn-primary" do %> <i class="fa fa-reply"></i> Voltar <%end%>
<% end %>
